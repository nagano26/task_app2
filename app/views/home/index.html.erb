<div class="top">
    <div class="top-main">
        <div class="conrainer">
            <div class="row col-lg-6">
                <div class="form-fream">
                    <div class="form-area">
                        <p class="form-area-title">
                        早速、お部屋探ししてみよう！！！
                        </p>
                        <p>
                        <%= form_with url: search_path, method: :get, local: true do |f| %>
                            <%= f.text_field :area, value: @area, :placeholder => "東京" %>
                            <%= f.submit "検索" %>
                        <% end %>
                        </p>
                    </div>
                </div>
            </div>
        
        </div>

    </div>
    <div class="container">
        <h1 class="section-title">おすすめのエリア</h1>
        <div class="item_field">
            <div class="area_item">
                <p class="area_title">東京</p>
                <%= form_with url: search_path, method: :get, local: true do |f| %>
                    <%= f.hidden_field :area, :value => "東京" %>
                    <%= image_submit_tag("https://presite-potepancamp-rails-02.herokuapp.com/assets/img_tokyo.jpg") %>
                <% end %>
            </div>
            <div class="area_item">
                <p class="area_title">大阪</p>
                <%= form_with url: search_path, method: :get, local: true do |f| %>
                    <%= f.hidden_field :area, :value => "大阪" %>
                    <%= image_submit_tag("https://presite-potepancamp-rails-02.herokuapp.com/assets/img_osaka.jpg") %>
                <% end %>
            </div>
            <div class="area_item">
                <p class="area_title">大阪</p>
                <%= form_with url: search_path, method: :get, local: true do |f| %>
                    <%= f.hidden_field :area, :value => "京都" %>
                    <%= image_submit_tag("https://presite-potepancamp-rails-02.herokuapp.com/assets/img_kyoto.jpg") %>
                <% end %>
            </div>
            <div class="area_item">
                <p class="area_title">大阪</p>
                <%= form_with url: search_path, method: :get, local: true do |f| %>
                    <%= f.hidden_field :area, :value => "札幌" %>
                    <%= image_submit_tag("https://presite-potepancamp-rails-02.herokuapp.com/assets/img_sapporo.jpg") %>
                <% end %>
            </div>
        </div>
    </div>
</div>
